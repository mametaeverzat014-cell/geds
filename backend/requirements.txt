fastapi>=0.115
uvicorn[standard]>=0.32
websockets>=13
pydantic>=2.9
pydantic-settings>=2.5
numpy>=1.26,<2.0       # pinned <2 for torch/pandas/sklearn/statsmodels compat
scipy>=1.14
pandas>=2.0,<3.0       # Comtrade ETL + DataFrame ops in app/data/comtrade_*.py
pyarrow>=14.0          # parquet engine for cache files
networkx>=3.4
msgpack>=1.1
orjson>=3.10
httpx>=0.27          # used at runtime by Grok client and news fetchers

# scientific rigor upgrade
optuna>=3.6          # Bayesian calibration

# news pipeline (optional; pipeline degrades gracefully without them)
spacy>=3.7           # NER (also need: python -m spacy download en_core_web_sm)
rapidfuzz>=3.5       # fuzzy node matching

# dev
pytest>=8
pytest-asyncio>=0.24
ruff>=0.7
